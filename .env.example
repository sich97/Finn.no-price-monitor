# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=alerts@gmail.com
EMAIL_TO=recipient@example.com

# Data directory (used by Docker)
DATA_DIR=/data

# Scheduling mode: 'once' to exit after run, 'loop' to wait and rerun
SCHEDULE_MODE=once

# Hours to wait between checks when in loop mode (1-168, default: 4)
CHECK_INTERVAL_HOURS=4
